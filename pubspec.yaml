name: fl_jpush
description:  jpush plugin for flutter, only Android and IOS are supported
version: 0.2.7
repository: https://github.com/Wayaer/fl_jpush.git

environment:
  sdk: '>=2.12.1 <3.0.0'
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: flutter.jpush
        pluginClass: JPushPlugin
      ios:
        pluginClass: JPushPlugin
